[project]
authors = [
  {email = "engineering@canvasmedical.com", name = "Canvas Team"}
]
dependencies = [
  "canvas[test-utils] @ https://github.com/canvas-medical/canvas-plugins/releases/download/0.136.0/canvas-0.136.0-py3-none-any.whl",
]
description = "Patient portal membership sign-up, recurring billing, and cancellation management."
license = "MIT"
name = "portal-membership"
readme = "portal_membership/README.md"
requires-python = ">=3.11"
version = "0.9.6"

[tool.pytest.ini_options]
python_files = ["*_tests.py", "test_*.py", "tests.py"]
