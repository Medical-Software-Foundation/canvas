[project]
authors = [
  {email = "engineering@canvasmedical.com", name = "Canvas Team"}
]
dependencies = [
  "canvas[test-utils]==0.169.1",
]
description = "Enriched schedule calendar with appointment type, labels, provider, room, and status at a glance."
license = "MIT"
name = "schedule-view"
readme = "schedule_view/README.md"
requires-python = ">=3.11"
version = "0.1.0"

[tool.pytest.ini_options]
python_files = ["*_tests.py", "test_*.py", "tests.py"]
