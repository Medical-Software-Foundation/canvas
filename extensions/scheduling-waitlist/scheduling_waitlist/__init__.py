"""Scheduling waitlist plugin."""

# Bumped whenever a served static asset changes. Appended to every asset URL so
# a browser never serves a stale roster.css / roster.js against new markup.
CACHE_BUST = "0.10.0"
