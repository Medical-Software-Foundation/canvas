[pytest]
# Run inside this plugin directory, ignoring the surrounding home-app pytest config
# and disabling pytest-django (the SDK calls django.setup() itself in
# canvas_sdk/__init__.py, so an additional pytest-django setup is redundant and
# would race on app-registry population).
testpaths = tests
addopts = --confcutdir=. -p no:django
